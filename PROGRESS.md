# 中文化進度

最後更新：2026-08-20（commit `1a2c67f`）

這份文件記錄各檔案目前翻到哪裡、下一步該做什麼。改完東西請順手更新這裡的數字，
數字都可以用各工具的 `--verify` 重新產生，不要用猜的。

## 一覽

| 檔案 | 狀態 | 備註 |
|---|---|---|
| `text/gui_translations.xml` | 結構完成，用詞待統一 | 五段皆無韓文、無簡體 |
| `text/korean.xml` | **3,615 / 4,345（83%）** | 剩 730 筆，七成字元是長篇戰報 |
| `text/tutorial_data.xml` | 完成 | KR 821 + KRGROUP 647 全中文 |
| `database/names.xml` | 完成 | 25.7 萬筆，一律退回英文原名 |
| `database/schools.xml` | 完成 | 台日人工翻譯，其餘英文 |
| `database/world_default.xml` | 完成 | 國家／洲／族裔／台日城市中文，其餘英文 |

## text/korean.xml — 主戰場

上線 4,345 筆、註解 52,311 筆。已完成的部分：

- **每分類一筆的 915 個分類** → 100%（逐球播報、獎項與賽事新聞稿）
- **場上播報** → 100%（CAT 185 球數與投球動作、911 球種、979 天氣、900 半局結算、
  896 打者近況、966 季後賽開場）
- **球探報告** → 100%（CAT 908/915/916/917/918/919/920/921/925/926/927/928/929/
  932/933/934/935/939/940，共 1,197 筆）
- **教練與總管評語** → 100%（CAT 750 總教練風格、751 個性、752 投手教練、
  753 打擊教練、755 總經理，共 481 筆）
- **老闆目標回饋** → 100%（CAT 786~811，戰績／升降級／位置補強／續約／薪資／
  獲利／進場人數／總教練名聲／新秀／農場，共 310 筆）
- **合約談判與零星分類** → 100%（CAT 723/724/762 球員還價與老闆代簽、
  774/776 季後賽門票與淘汰、1104/1105 團隊化學效應、399 徵才信、903 自訂獎項、
  661 鬥毆禁賽、432 里程碑全壘打、652 盜壘場、650 三振場、379 多轟場）

### 還沒翻的 730 筆 / 280,099 字元

| 分類 | 筆數 | 字元 | 內容 |
|---|---|---|---|
| 860 | 320 | 146,845 | 季後賽戰報，每筆帶 8~12 個 `<COND>` |
| 829 | 169 | 32,268 | 例行賽戰報 |
| 840 | 94 | 22,347 | 賽後新聞稿 |
| 377 | 40 | 26,175 | 單場多安／大爆發長文 |
| 654 | 26 | 14,458 | 連續安打場次 |
| 831 | 19 | 3,598 | 勝投投手數據段落（很短，可先撿） |
| 382 | 18 | 11,884 | 打擊表現長文 |
| 24 | 16 | 11,432 | 單場三響砲 |
| 839 | 16 | 4,544 | 開幕戰戰報 |
| 656 | 12 | 6,548 | 連續安打中止 |

### 建議順序

1. **CAT 831 的 19 筆**。平均只有 189 字元，是純數據句型（「X 拿下勝投，戰績來到
   A-B」），十分鐘可以清掉。
2. **CAT 839 開幕戰 16 筆**。每年球季開始都會看到。
3. **CAT 656 / 654 連續安打 38 筆**。
4. **CAT 24 / 377 / 382 共 74 筆**。單場爆發的長文。
5. **長篇戰報最後做**（860 + 829 + 840 = 583 筆），佔剩餘字元的 72%，
   但屬於看過就滑掉的內容。其中 **CAT 860 一段就 320 筆 / 146,845 字元，
   是全部剩餘的一半以上**。

### CAT 860 值得先評估能不能再精簡

CAT 860 是季後賽戰報，每筆帶 8~12 個 `<COND>`。目前的規則是「每種條件組合各留
一筆」，所以留了 320 筆。如果遊戲在找不到匹配時會優雅退回（例如退到 CAT 829 的
一般戰報），就可以只留主要幾種組合，一口氣省掉一半的工作量。這件事沒辦法從
檔案靜態判斷，要實際在遊戲裡試。

### 怎麼繼續

```
python3 tools/merge_korean.py --todo | awk -F'\t' '$1=="831"'   # 列出某分類還沒翻的
python3 tools/apply_zh.py batch.tsv --check                      # 只檢查譯文不寫檔
python3 tools/apply_zh.py batch.tsv                              # 套用
python3 tools/merge_korean.py --verify                           # 檢查並看進度
```

翻好的東西寫成 `id<TAB>中文` 一行一筆餵給 `apply_zh.py` 就好，它會連被註解掉的
同 id 一起換，所以重跑 `merge_korean.py` 不會把翻譯洗掉。

**寫檔前 `apply_zh.py` 會自動擋掉這四項**，之前每一項都真的抓到過錯：

- `[%...]` 佔位符進出數量與內容完全一致（連 `[%Personname F L]` 的大小寫都算）
- `(nl)` 數量相同
- 值裡不能有 `"`（會破壞 XML 屬性）
- 去掉佔位符與 `(nl)` 後不能殘留英文單字（`BNN` 例外，見 `KEEP_EN`）

另外還會檢查 `{He|She|They}` 這種性別分支：中文常常整句不用代名詞，少寫幾個沒
關係，但寫出來的每一個分支數都要對得上英文。中文一律寫成
`{他|她|他們}` / `{他的|她的|他們的}`。

## text/gui_translations.xml — 剩用詞統一

五個段落都已經沒有韓文和簡體字，`--verify` 全綠。剩下兩件事：

**1. 對岸用語換成台灣用語**（`OPENCC_CONFIG` 從 `s2tw` 改成 `s2twp` 可解決大部分）

```
設置 406  數據 364  啓 171  信息 141  用戶 84  線上 75  激活 70  週 64  裏 57
質量 35  搜索 35  項目 27  程序 25  磁盤 17  網絡 16  字體 10  內存 10  視頻 6
```

注意這會動到 17,000 多筆既有翻譯，而且 `數據`、`項目`、`交易` 在棒球語境下有些
是對的，要逐項確認再套用。

**2. 213 條刻意留英文的可讀字串**（`HoF Induction`、`Black Ink`、`ERA Crown` 等）。
另有約 1,175 條是 `%a OBP`、`SV`、`K9` 這類數據欄位縮寫，那些本來就該留英文。

`TEAM_ABBR` 整段是三碼英文縮寫，不用動。

## 術語表

跨檔案統一用這些譯法，新翻的請對齊：

| 英文 | 中文 | | 英文 | 中文 |
|---|---|---|---|---|
| stuff | 球威 | | waiver | 釋出 |
| control | 控球 | | posted player | 發佈球員 |
| command | 球路掌握度 | | draft | 選秀 |
| movement | 尾勁 | | draft lottery | 選秀抽籤 |
| eye / plate discipline | 選球眼 / 打擊區紀律 | | minors | 小聯盟 |
| stamina | 續航力 | | opening day | 開幕日 |
| out pitch | 決勝球 | | Perfect Team | 完美球隊 |
| back of the rotation | 輪值末端 | | windup / set position | 揮臂式 / 固定式 |
| setup man | 布局投手 | | keystone / hot corner | 二壘 / 三壘 |
| swingman | 先發後援兩用 | | on-base plus slugging | 整體攻擊指數 |
| spot starter | 臨時先發 | | Ni-Gun | 二軍 |
| sabermetrics | 賽伯計量學 | | smallball | 小球戰術 |
| power / finesse pitcher | 強力型／技巧型投手 | | groundball pitcher | 滾地球型投手 |
| contact / power hitter | 接觸型／長打型打者 | | farm system | 農場系統 |
| face of the franchise | 看板人物 | | reputation | 風評／名聲 |

## 已知的坑

- **XML 註解不能有連續兩個 `-`**，而內文破折號正是寫成 `--`。`merge_korean.py`
  會在連續的 `-` 之間補空白，只影響註解內容。用 `replace("--", "- -")` 會漏掉
  `---`，要用 `re.sub(r"-(?=-)", "- ", ...)`。
- **上游原始檔的 KR/ES/JP/CN 欄位實體逸出了兩次**（`<EN>` 寫 `&#39;`、`<CN>` 寫
  `&amp;#39;`），照抄的話遊戲會直接印出 `&#39;`。`merge_gui.py` 會還原一層。
- **不能對已經是繁體的文字跑 s2tw**，會把 `游擊手` 改成 `遊擊手`、`系統` 改成
  `係統`、`場合` 改成 `場閤`。只轉真正未翻譯的條目。
- **OpenCC 認為 `群`/`才` 的繁體是 `羣`/`纔`**，但台灣標準字就是 `群`/`才`。
  簡體字偵測要扣掉 `TWVariants` 的還原目標才不會誤報。
- **`merge_schools.py` 與 `merge_world.py` 沒有 `--verify`**，其餘三支有。
- **`BNN` 在 `gui_translations.xml` 裡是留英文的**，`korean.xml` 要跟著留，
  不要翻成「棒球新聞網」。

## 待驗證

- 遊戲實測已確認 `korean.xml` 精簡（每分類一筆）沒有問題。
- CAT 860 能不能再精簡，尚未驗證，見上。
